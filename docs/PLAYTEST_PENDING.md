# Playtest Pending

Single-source sink for cycles that closed under
`posture: autonomous-loop` (per
[docs/CAMPAIGN_2026-05-13-POST-WEBGPU.md](archive/CAMPAIGN_2026-05-13-POST-WEBGPU.md))
and merged on CI green + reviewer APPROVE without the owner-playtest
gate firing.

The orchestrator appends a row here at every cycle close that had a
playtest-required task. The owner walks the deferred items in batches
after the campaign completes (or mid-campaign during a planned
break).

If a deferred playtest rejects the merged work, the owner opens a
follow-up cycle (or a single hotfix task) — the merged commit is
NOT reverted automatically.

## Active deferrals

| Cycle slug | Close commit | What to walk | Playwright smoke screenshots | Notes |
|------------|--------------|--------------|------------------------------|-------|
| `2026-06-10-owner-feedback-fixes` | (this fix's commit) | Three owner-reported fixes from the 2026-06-10 session: (1) **Night foliage hue** — A Shau or OF at midnight (worldbuilder `forceTimeOfDay`): vegetation and NPCs should read as DARKER GREEN (hue preserved), not grey; the rig ambient now multiplies albedo like terrain instead of painting a flat grey-blue over the texture. (2) **Ground-vehicle follow camera** — board the OF M151 and M48: the third-person view now looks FORWARD over the hood/turret at the terrain ahead (~5-6° view pitch) instead of pitching ~45° down onto the chassis; confirm the area ahead is visible while driving. (3) **GLB vehicle visuals** — the drivable M151 and M48 now render the real `m151-jeep.glb` / `m48-patton.glb` instead of procedural boxes; check orientation (front is front), scale vs NPCs, ground contact, and that the M48 turret + barrel still traverse/elevate with gunner aim (GLB `Joint_Turret`/`Joint_MainGun` re-seated on the TankTurret rig). | `artifacts/playtests/land-vehicle-runtime-proof/` (refreshed this session — camera band + GLB visuals visible in the after-board/after-drive shots); night foliage: `artifacts/lighting-rig/tod-sweep/gate/tod-*.png` after `npm run check:tod-coherence`. | Camera framing is now GATED as a 15-40° down-angle band in `check-land-vehicle-runtime.ts` (was min-45° — the over-correction from the old camera-under-car bug). Procedural vehicle meshes remain as load-failure fallbacks only. The night-hue fix is the engineering response to the owner's first partial verdict on the `cycle-2026-06-09-lighting-acceptance` walk (night darkness YES, foliage hue NO). |
| `cycle-2026-06-09-lighting-acceptance` | (this cycle's close commit) | **Lighting-rig campaign acceptance walk — the campaign's exit gate, on PROD after the next deploy**: full day/night cycle on A Shau (worldbuilder `forceTimeOfDay` or natural cycle): (1) dawn terrain — NO near-white reflection (the original defect); (2) foliage tracks terrain lighting through the whole cycle instead of holding near-constant; (3) midnight genuinely dark across terrain+foliage+NPCs; (4) NPC readability per faction at dawn/noon/dusk/night (parity uniforms are now inert; gate corr 0.997 says impostors track, but readability is the owner's call); (5) scenario spot-check: OF + TDM + ZC each at one TOD (preset trims: tdm cooler/dimmer, zc warmer; combat120 untinted by design). Kill-switch if anything looks wrong: `window.__lightingRig.enabled = false` (one release only). | 8-TOD gate set regenerable: `npm run build:perf && npm run check:tod-coherence` → `artifacts/lighting-rig/tod-sweep/gate/tod-*.png` (dawn 06h / noon 12h / dusk 17h / midnight 00h). | Campaign PRs #363-#381 (Phases 0-4). Phase 4: #380 standing gate; #381 rig default-ON + legacy deletion (−405 LOC). combat120 p99 IMPROVED 38.8→33.6ms (same-machine A/B). GLB rangeRatio 6.6× advisory-flagged (direct-sun swing on vehicle bodies, never the defect) — promote to hard gate later if wanted. A NO = hotfix cycle; the flag stays flippable for one release. |
| `cycle-2026-06-09-helicopter-craft` | (this cycle's close commit) | **Phase 3 exit feel-walk — closes the craft-specialization campaign**: (1) board the UH1C gunship, press F mid-flight to take the door gun — camera moves to the door-side gunner POV, `door_gun` reticle up, mouse aim hard-stops at the mount arc (edge ticks light at the stops), LMB fires the M60 with tracers, belt count on the gunship panel; F back to pilot — clean restore, no view leakage; (2) the unarmed UH-1 transport: F toggle is a no-op, HUD shows NO weapon panels (per-variant descriptors); (3) Cobra: cycle gun↔rockets — reticle prominence swaps; in level flight the rocket cue sits below the pipper, in a dive it tucks toward it; fire a rocket run using the cue; weapon name + ammo live on the HUD. | Engineering evidence: deterministic CCIP tests (level hover → drop, dive → converge), variant→panel matrix tests, POV swap/restore tests, arc-clamp tests. No live capture script. | 3 PRs #374 #375 #377; no combat-path files touched; zero fence changes across the whole campaign (11 PRs). Campaign follow-ups in BACKLOG entry. |
| `cycle-2026-06-09-fixed-wing-craft` | (this cycle's close commit) | **Phase 2 exit feel-walk of the craft-specialization campaign**: (1) take the A-1 — reflector gunsight visible, strafe run using the sight (reticle sits on the wing-cannon convergence), ammo counter at 480 decrementing, LOW under 20%; (2) F-4 pass — rotary feel (24 rds/s, mag 640), farther/faster chase cam with speed-FOV widen; (3) AC-47 — NO nose gun anymore; RMB toggles the left-side broadside gunner view (camera looks 90° left with down-angle, reticle centered on the broadside convergence); orbit a zone and walk tracers with the 3-minigun battery (mag 1500); RMB back restores the chase cam, exit leaves no FOV/view leakage. Each airframe should feel DISTINCT. | Engineering evidence: NDC convergence-alignment tests in PlayerCamera.test.ts; per-airframe fire-geometry tests in FixedWingArmament.test.ts (broadside fires ~90° left). No live capture script this cycle. | 3 PRs #370 #372 #373; no combat-path files touched (no reviewer gate). Phase 3 (helicopter) already dispatched under the /goal interleave authority. |
| `cycle-2026-06-09-lighting-rig-spike` | (this cycle's close commit) | **Phase 0 GO/NO-GO review of the lighting-rig campaign** (`docs/CAMPAIGN_2026-06-09-lighting-rig.md`). Read the design memo (`docs/rearch/LIGHTING_RIG_SPIKE_2026-06-09.md`) and view the A/B TOD-sweep captures (`artifacts/lighting-rig/tod-sweep/{rig-off,rig-on}/`, regenerable via `npm run capture:tod-sweep`). Orchestrator's recorded verdict: **GO** — the clamp bypass works (foliage min luminance 0.129→0.012, range ratio vs terrain 0.290→1.564, inside the memo band); the one miss (foliage correlation 0.533 vs ≥0.92) is structural — terrain still stacks legacy scene lights on rig terms, which is Phase 1's exact scope — so `scene-light-unification` was re-scoped as a hard co-requisite of `terrain-rig-migration`. Also confirm midnight rig-path terrain isn't crushed unreadably dark (terrain-nav reviewer note). | `artifacts/lighting-rig/tod-sweep/rig-off/` + `rig-on/` (8 PNGs + curves.json each, in the rig-prototype worktree; regenerate on master with the flag toggled via `window.__lightingRig.enabled`). | 3 PRs: #363 memo, #365 harness, #368 prototype (terrain-nav APPROVE-WITH-NOTES: dead ALU on the OFF path until Phase 4 deletion; terrain→environment binding coupling goes on the Phase 4 deletion checklist). An owner NO-GO halts the campaign before Phase 1 does mass migration. |
| `cycle-2026-06-09-ground-gunnery-craft` | (this cycle's close commit) | **Phase 1 exit feel-walk of the craft-specialization campaign** (`docs/CAMPAIGN_2026-06-09-craft-specialization.md`): (1) board the OF motor-pool M48, swap to the gunner seat — confirm the stadia sight reticle + FJ gunner panel (READY/RELOADING + reload bar + azimuth dial + magnification) appear and the camera snaps to the down-barrel sight POV (NOT the old orbit cam); (2) RMB toggles ~2.8x zoom; kill a target using the sight alone; swap to driver — sight/panel drop, orbit cam returns; (3) crew the OF US FOB M2HB — MG reticle + belt counter (250 down with fire, refills on remount), camera recoil per shot, traverse-stop cue at the pitch stops; walk tracers onto a target; (4) take return CANNON fire from an NPC-crewed M48 (new — NPC tanks now shoot back; pre-cycle they never fired). | Engineering evidence: L3 suites `npc-tank-cannon.test.ts` (incl. single-owner stepping + dt=0 pause freeze), `m48-board.test.ts` (board→swap→zoom→exit), `EmplacementGunExperience.test.ts`; no live capture script this cycle. | 5 PRs: #362 reticle-framework, #364 npc-tank-cannon-wiring (combat-reviewer CHANGES-REQUESTED → double-stepping fix → merged), #366 tank-gunner-sight, #367 m2hb-gun-experience, #369 tank-sight-prod-wiring (orchestrator-inline; also fixed PlayerCamera never calling any gunner-sight pose). Phase 2 (fixed-wing) opens after this walk per the campaign's feel-walk barrier — or earlier on owner instruction. |
| `cycle-2026-06-09-terrain-fidelity-and-worker-safety` | (this cycle's close commit) | Phase 4 of the consultation-remediation campaign — walk A Shau Valley: (1) **NPC slope movement** — watch squads traverse steep valley walls; the contour-oscillation stall (NPCs ping-ponging on slopes) should be visibly reduced or gone (heightmap fix + full-map navmesh); pay attention to the map perimeter ~1.7km band beyond the outermost zones, which previously had no navmesh at all (NPCs beelined). (2) **CPU↔render ground mismatch** — on the sharpest ridgelines, check whether the player/NPCs visibly float or sink vs the rendered terrain (bounded: mean 0.86m, max 18m on rare sharp-ridge cells; if it reads badly, the follow-up is lifting the GPU surface grid to 1024, +3MB GPU). (3) **Mode-switch robustness** — rapidly enter/exit A Shau and other modes; startup should never wedge (worker lifecycle hardening). | Engineering evidence: contour-flip measurement on the real DEM in PR #354; `NavmeshAShauCoverage.test.ts` perimeter-band paths; combat120 captures `artifacts/perf/2026-06-09T21-57-12-684Z` (pre) vs `2026-06-09T22-29-00-825Z` (post-heightmap: stall-warning storm gone, avg 24.0→20.9ms, max 63.5→46.6ms). | 4 PRs #352-#355, all terrain-nav APPROVE-WITH-NOTES. If slope movement reads well here, `combat-movement-stall-tail` is a candidate to close and the default-OFF F2 sticky-contour-hysteresis flag may be unnecessary. |
| `cycle-2026-06-09-combat-death-and-alliance` | (this cycle's close commit) | Phase 3 of the consultation-remediation campaign — combat-rhythm deltas to feel out in a 120-NPC firefight (Open Frontier or combat120): (1) NPCs behind intermittent cover now re-attempt fire as soon as LOS clears instead of eating a full fire-rate interval per blocked attempt — firefights at treelines/ridges should feel snappier without feeling unfair; (2) squads whose leader dies promote a survivor and fully-dead squads disappear — late-match squad cohesion should read better (no leaderless wandering clumps); (3) zones reclaim defender slots from the dead — late-game zones should keep getting fresh defenders; (4) allied factions (US+ARVN / NVA+VC) no longer suppress each other. Bodies should despawn exactly as before (timing unchanged) with no immortal corpses and no popping mid-death-animation. | N/A — covered by L3 behavior tests + the combat120 perf capture pair (`artifacts/perf/2026-06-09T20-50-12-389Z` pre vs `2026-06-09T21-57-12-684Z` post: p99 50.6→~31ms). | 6 PRs #346-#351, all combat-reviewer gated. Watch raycast-budget denial rate (`getCombatFireRaycastBudgetStats`) if firefights feel off — blocked NPCs now re-poll the terrain gate every frame by design, absorbed by the adaptive cap. |
| `cycle-2026-06-09-vehicle-occupancy-truth` | (this cycle's close commit) | Phase 2 of the consultation-remediation campaign. (1) **High-refresh display (120/144Hz):** drive the M48 and confirm the hull renders smoothly (render-time interpolation landed; raw fixed-step pose was the jitter cause — same class as the fixed heli bug). (2) **Seated fire:** board the M48, hold LMB — cannon fires with reload gating; mount the OF US FOB M2HB, hold LMB — it fires at rate. Both were dead end-to-end before this cycle (input probes + composer wiring both missing). Space also fires. (3) **Seat lifecycle:** board any vehicle, exit with Escape (not F), re-board — must land in the same seat with no "occupied by ghost" refusal; also enter a heli via its interaction prompt and confirm you are the pilot. (4) **Position truth:** drive the jeep 500m+ — terrain chunks must keep streaming around you, minimap blip and zone-capture presence must track the vehicle, and NPCs must shoot at the vehicle, not the boarding spot. | `artifacts/playtests/land-vehicle-runtime-proof/` refreshed on the post-cycle tree 2026-06-09 (OF M151+M48 board/drive/camera/exit, weapon-overlay suppression, 0 browser errors, all 11 PASS). Cannon/M2HB fire is L3-proven (`src/integration/vehicle/seated-weapon-fire.test.ts`); no live-fire capture script yet. | 5 PRs: #341 tank-interpolation, #342 vehicle-seat-lifecycle, #343 tank-cannon-wiring (combat-reviewer APPROVE-WITH-NOTES), #344 vehicle-player-position-sync, #345 watercraft-camera (boats dormant — wiring verified by test only). Reviewer follow-ups tracked in cycle retro: Escape-exit path bypasses the factory detach hook (defended by `mounted` guards), M2HB one-frame latch ordering lag, in-flight cannon round freezes on dismount (MVP caveat), NPC tank cannon still unwired in prod. |
| `helicopter-chase-cam-jitter-fix` | (this fix's commit) | **High-refresh display required (120/144Hz).** Pilot a helicopter on Open Frontier or A Shau and confirm the airframe no longer shakes/snaps back-and-forth while the flight path stays smooth — both in follow mode and free-orbital look (`Right Ctrl`). Spot-check that the fixed-wing chase cam and the ground-vehicle (M151/M48) third-person follow are still smooth (same `SystemUpdater` order drives all three). The bug was a one-frame camera/model desync; it is INVISIBLE at 60Hz, so a 60Hz monitor cannot confirm the fix. | N/A — high-refresh-only visual artifact; not reproducible in headless 60Hz capture. Locked instead by an L1 update-order guard in `src/core/SystemUpdater.test.ts` ("updates Vehicles before Player ..."). | Root cause: `454c1fec` reordered `SystemUpdater` so the Player block (camera) ran before the Vehicles block (which publishes the interpolated pose that `PlayerCamera` hard-copies). Fix restores Vehicles-before-Player + a load-bearing comment. The reorder was incidental to that commit (weapon suppression is event-driven; ground-vehicle keybinds live in `PlayerInput.ts`), so the revert regresses neither. |
| `cycle-terrain-vehicle-water-foundation-reset` | (this cycle's close commit) | Walk Open Frontier, A Shau Valley, and Zone Control after the foundation reset: (1) M151/M48 spawn on believable playable terrain, not clipped into slopes; (2) US and NVA M48s are findable, rest on terrain, and take damage; (3) Zone Control home bases and capture points sit on playable pads with no ditch drift; (4) drive/board/dismount enough to flag remaining vehicle feel issues. Ground controls: F board/exit/seat swap, W/S throttle, A/D steer or skid-steer, Space brake, LMB fire where armed. Current proven anchors: Open Frontier M151 `m151_jeep_open_frontier_motor_pool` near `(176, terrain, -1188)`, Open Frontier M48 `m48_tank_of_us_fob` near `(183, terrain, -1173)`, A Shau M151 `m151_jeep_ashau_tabat_motor_pool` near `(-702, terrain, -5034)`, and A Shau M48 `m48_tank_ashau_valley_road` near `(40, terrain, 60)`. While seated in any vehicle, there must be no first-person infantry rifle/weapon overlay visible. NOTE: the water half of this reset (basin bodies / hydrology) was stripped to first principles on 2026-06-09; water + watercraft drop out of this walk and return when water is reworked in a future terrain/world-generator cycle that re-introduces a water level + real-time debug visualization. | Current land-vehicle proof: `artifacts/playtests/land-vehicle-runtime-proof/land-vehicle-runtime-proof.json`, `open_frontier-m151-after-drive.png`, `open_frontier-m48-after-drive.png`, `a_shau_valley-m151-after-drive.png`, and `a_shau_valley-m48-after-drive.png`. Foundation screenshots: `artifacts/playtests/terrain-vehicle-water-foundation-reset/open_frontier-m48-terrain-bind.png`, `a_shau_valley-m48-terrain-bind.png`, `zone_control-us_base-pad.png`, `zone_control-opfor_base-pad.png`, `zone_control-objective-overview.png` | `scripts/check-land-vehicle-runtime.ts` now proves board/drive/camera/exit and fails if a first-person weapon overlay root or mesh remains renderable while seated. Automated gates prove bounded terrain/vehicle placement but do not replace owner feel review. (Water-runtime checks were removed in the 2026-06-09 water strip.) |
| `cycle-sky-visual-restore` | (this cycle's close commit) | Superseded by SOL-1 owner review. Walk noon/golden/dusk/night after the SDS-style retune: the visible body should read as a hot fire object with a broad warm core, fractured amber shell, and tight warm surrounding solar mass; low-angle light must not look like a giant damp sphere or bleed through hills; WebGPU vs `?renderer=webgl` should remain visually comparable; A Shau midnight water should read as cool level/depth water instead of a red surface slab. Use [docs/directives/sol-1-acceptance-packet.md](directives/sol-1-acceptance-packet.md) as the review packet. | Historical evidence: `artifacts/cycle-sky-visual-restore/playtest-evidence/sky-dome-tonemap-and-lut-resolution-noon.png`, `sky-hdr-bake-restore-{webgpu,webgl}.png`, `sky-sun-disc-restore-{noon,nadir}.png`. Current proof evidence: `artifacts/cycle-sun-and-atmosphere-overhaul/playtest-evidence/summary.json`, `ridge-summary.json`, `visual-openfrontier-golden.png`, `visual-ashau-midnight.png`, and `ridge-ashau-dusk-{webgpu-strict,webgpu-force-webgl}.png`. Live proof is `npm run check:live-release`. | Three R1 PRs merged (#208 `2118177f`, #210 `3455fa96`, #209 `9e1ce7c7`). SOL-1R8 moved the current acceptance target to deployed engineering proof: full matrix 33/33, WebGPU/WebGL2 parity max channel delta 0.39%, A Shau ridge parity 0.00%, and A Shau midnight night-terrain localMax red/white/cyan/bright 0.0%. Owner visual acceptance remains pending. |
| `cycle-mobile-webgl2-fallback-fix` (task `asset-audio-defer`) | (this task's close commit) | First-shot audio: fire a weapon within the first ~2 s of playable frame on desktop AND on a real mobile device. Confirm gunshot SFX plays without a perceptible gap. The startup marks `systems.audio.background.end` lands at ~30.7 s in mobile-emulation while playable frame is ~47 s — so the SFX bank is decoded long before first shot in emulation; real-device timing should be similar or better. | N/A (audio-presence assertion not automated this cycle) | Brief allowed deferring this to PLAYTEST_PENDING under autonomous-loop posture. Automated assertion shape (Playwright fires simulated input + asserts on `console.log`/`performance.mark`) was judged too heavyweight for this single-task scope. Startup-marks evidence + `whenSfxReady()` test coverage stands in for the automated check. |
| `cycle-mobile-webgl2-fallback-fix` (real-device walk-through) | (this cycle's close commit) | Run `scripts/real-device-validation.ts --device=android-chrome-debug --ws-endpoint=<ws>` against a real Android Chrome 120+ mid-tier 2022+ phone after `adb reverse tcp:4276 tcp:4276`. Acceptance: `steadyState.avgFps ≥ 30`. Then run `scripts/real-device-validation.ts --device=ios-safari-manual --ios-input=<json>` after harvesting numbers via Safari Remote Inspector on a real iPhone (iOS Safari 17+). Acceptance: owner "playable" sign-off. Confirm `resolvedBackend` discriminates "real mobile gets WebGPU" vs "real mobile gets WebGL2 fallback" — load-bearing for engine-trajectory direction. | `artifacts/cycle-mobile-webgl2-fallback-fix/playtest-evidence/pixel5-emulation/summary.json`, `artifacts/cycle-mobile-webgl2-fallback-fix/playtest-evidence/iphone12-emulation/summary.json` — Pixel 5 emulation 23.68 avgFps, iPhone 12 emulation 28.30 avgFps, both 60 s steady-state. | Real-device validation deferred under autonomous-loop posture; harness script ready for owner-attach run; emulation smoke stands in for cycle merge gate. Memo at `docs/rearch/MOBILE_WEBGPU_AND_SKY_SPIKE_2026-05-16/cycle-close-validation.md` records full owner-attach procedure for both platforms. Closes carry-over `KB-MOBILE-WEBGPU` on cycle close. |
| `cycle-vekhikl-1-jeep-drivable` | (this cycle's close commit) | Drive the M151 on Open Frontier and A Shau: (1) flat-ground acceleration to cruise; (2) slope traversal with per-wheel terrain conform; (3) Ackermann U-turn at cruise; (4) slope-stall on the steepest grade; (5) brake-stop from cruise; (6) F-key enter/exit with player ejected to the side of the chassis (not inside, not under terrain). Camera should be third-person follow while driving. Full walk-list mirrored in `docs/playtests/cycle-vekhikl-1-jeep-drivable.md` "What the owner should walk". | `artifacts/cycle-vekhikl-1-jeep-drivable/playtest-evidence/jeep-spawn-open-frontier.png`, `jeep-spawn-a-shau.png`, `jeep-driving-from-third-person.png` (captured by `scripts/capture-m151-jeep-playtest-shots.ts`; may require post-merge back-fill if sibling integration PRs landed after this task). | First playable ground vehicle. Owner sign-off blocks DIRECTIVES.md VEKHIKL-1 promotion to Closed. Capture-script + doc skeleton landed under autonomous-loop posture; screenshots back-fillable post-merge per the renderer-backend + sibling-PR caveats in the playtest memo. |
| `cycle-vekhikl-2-stationary-weapons` | (this cycle's close commit) | (1) Approach the M2HB emplacement at the Open Frontier US base. (2) F to mount; confirm first-person camera pins to the gunner seat behind the spade grips. (3) Mouse-aim — barrel slews yaw and pitch within the cone limits (-10°/+60° pitch, 360° yaw on the default tripod). (4) LMB fires at ~575 RPM with tracer every 5th round; recoil offset visible per shot. (5) Hold-fire to depletion of the 250-round box; confirm reload triggers on dismount, not mid-mount. (6) Move to A Shau and repeat on the NVA bunker overlook emplacement. (7) Stand near a friendly-faction emplacement during a firefight; confirm a friendly NPC mounts and engages enemies inside the cone within ~5 s. Full walk-list mirrored in `docs/playtests/cycle-vekhikl-2-stationary-weapons.md` "What the owner should walk". | `artifacts/cycle-vekhikl-2-stationary-weapons/playtest-evidence/emplacement-spawn-open-frontier.png`, `emplacement-spawn-a-shau.png`, `emplacement-third-person-aiming.png` (captured by `scripts/capture-vekhikl-2-emplacement-shots.ts`; emplacement mesh visibility depends on sibling `m2hb-weapon-integration` PR landing — back-fill on master post-merge if needed). | First stationary heavy weapon. Owner sign-off blocks DIRECTIVES.md VEKHIKL-2 promotion to Closed. Capture-script + doc skeleton landed under autonomous-loop posture; capture script tolerates an absent emplacement-specific spawn helper and an absent `adapter.setAim` aim-control method, so it remains runnable across the full R2 dispatch window. NPC-gunner walk-step depends on sibling `emplacement-npc-gunner` PR landing. |
| `cycle-vekhikl-3-tank-chassis` | (this cycle's close commit) | Drive the M48 Patton on Open Frontier and A Shau: (1) forward/reverse cruise; (2) in-place skid-steer pivot with zero forward translation (load-bearing feel test); (3) slope crest with four-corner hull tilt; (4) slope-stall on the steepest grade; (5) brake-stop; (6) F-key enter/exit with player ejected to the side of the chassis; (7) tracks-blown debug command — confirm forward throttle no longer produces motion but turret/chassis/camera remain functional. Record subjective feel on skid-steer responsiveness, hull tilt on slopes, and track-driven yaw rate (must feel tracked, not Ackermann-wheeled). Full walk-list mirrored in `docs/playtests/cycle-vekhikl-3-tank-chassis.md` "What the owner should walk". | `artifacts/cycle-vekhikl-3-tank-chassis/playtest-evidence/tank-spawn-open-frontier.png`, `tank-spawn-a-shau.png`, `tank-driving-third-person.png`, `tank-pivot-in-place.png`, `tank-on-slope.png` (captured by `scripts/capture-vekhikl-3-tank-shots.ts`; chassis mesh visibility depends on sibling `tank-player-adapter` + `m48-tank-integration` R2 PRs landing — back-fill on master post-merge if needed). | First tracked vehicle. Owner sign-off blocks DIRECTIVES.md VEKHIKL-3 chassis-half promotion to Closed (full directive closes after cycle #9 turret + cannon). Capture-script + doc skeleton landed under autonomous-loop posture; capture script tolerates absent tank-specific spawn helper, absent `setTrackInputs` adapter method, absent tracks-blown debug surface, and absent M48 mesh in scene — each capture reserves its screenshot path regardless. Tracks-blown walk-step depends on the debug command surface landing in `m48-tank-integration` or `tank-player-adapter`. |
| `cycle-vekhikl-4-tank-turret-and-cannon` | (this cycle's close commit) | Drive M48 on Open Frontier and A Shau and exercise the full combat platform: (1) mount pilot + drive ~30 m; (2) swap to gunner seat (first-person barrel-axis sight); (3) aim cannon + fire at static target — confirm slew cap reads correctly and projectile is visibly trackable in flight (~400 m/s muzzle); (4) observe arc + impact at ~280-500 m; (5) take hits to walk HP-band transitions (100% → 66% smoke wisps → 33% smoke plume → on-fire → wreck); (6) trigger each damage substate via dev console — tracks-blown (immobilize), turret-jammed (no slew), engine-killed (no throttle); (7) observe NPC tank gunner engagement on parked friendly tank (uses WASM solver for lead-prediction if pilot landed). Repeat on A Shau Valley. Record WASM-pilot verdict (KEEP / INCONCLUSIVE / REVERT) using the benchmark numbers from the `tank-ballistic-solver-wasm-pilot` PR description. Full walk-list mirrored in `docs/playtests/cycle-vekhikl-4-tank-turret-and-cannon.md` "Test plan". | `artifacts/playtests/cycle-vekhikl-4/tank-spawn.png`, `tank-drove-forward.png`, `tank-gunner-view.png`, `tank-turret-aimed.png`, `tank-projectile-apex.png`, `tank-projectile-impact.png`, `tank-on-fire.png`, `tank-tracks-blown.png`, `tank-turret-jammed.png`, `tank-engine-killed.png` (captured by `scripts/capture-vekhikl-4-tank-shots.ts`; cannon-fire / on-fire / substate capture visibility depends on sibling `tank-damage-states` + cannon-fire-wiring landing — back-fill on master post-merge if needed). | Full M48 combat platform (closes turret + cannon half of VEKHIKL-3 + entirety of VEKHIKL-4). Owner sign-off blocks DIRECTIVES.md VEKHIKL-3 (full) + VEKHIKL-4 promotion to Closed. Capture-script + doc skeleton landed under autonomous-loop posture; capture script tolerates absent gunner-swap helper, absent `setTargetYaw`/`setTargetPitch` aim surface, absent cannon-fire trigger, absent damage / substate dev commands — each capture reserves its screenshot path regardless. WASM pilot verdict is recorded in the playtest memo's "WASM pilot conclusion" section; verdict is required for sign-off but the cycle does not require KEEP (REVERT is acceptable per the pilot success bar). |
| `cycle-voda-3-watercraft` | (this cycle's close commit) | **No walk needed — boats are dormant.** Hydrology + all water (rendering, query/physics, swimming, authored basins) were stripped to first principles on 2026-06-09; watercraft code (Sampan, PBR) is kept dormant and boats no longer spawn. The Sampan + PBR walk returns once water is reworked in a future terrain/world-generator cycle that re-introduces a water level + real-time debug visualization. | N/A (boats not spawned; historical watercraft capture script removed in the 2026-06-09 strip). | Watercraft row parked pending the water rework. The buoyancy/sampler types were relocated to `src/systems/vehicle/WatercraftBuoyancyTypes.ts` so the watercraft code returns intact when water is rebuilt. |
| `cycle-vehicle-wayfinding-and-prompts` | (this cycle's close commit) | Walk all five drivable vehicle types (M151, M48, Sampan, PBR, M2HB) on the scenario where each spawns (Open Frontier for M151+M48+M2HB; A Shau for Sampan+PBR): (1) approach within 6 m and confirm the HUD "Press F to board <vehicle>" prompt fires with the correct per-vehicle copy; (2) step out to ~12 m and confirm the prompt hides; (3) press F to enter and confirm the prompt hides on entry; (4) exit and confirm the prompt returns; (5) glance at the minimap and confirm the per-category glyph (ground / watercraft / emplacement) renders at the vehicle world position with the faction palette (US blue today; OPFOR red gated on the `cycle-vekhikl-5-fleet-expansion` follow-up); (6) open the M-key full map and confirm the same glyph at the same world position under the north-up projection; (7) confirm the compass rose paints a `G/W/E <dist>m` chevron for the nearest vehicle of each category (compass-wiring fix landed in commit 1 of this branch — without it the chevrons stay dark); (8) drive a jeep and confirm the minimap marker tracks; (9) destroy a vehicle (dev command) and confirm its marker drops. NOTE 2026-05-20: the (3) "press F to enter" step is known-broken on master — F is not wired to any boarding handler. This deferral row is informational only for that sub-step until `cycle-vekhikl-player-boarding-wire` of `campaign-2026-05-20-vehicle-boarding-and-water` lands. Full walk-list mirrored in `docs/playtests/cycle-vehicle-wayfinding-and-prompts.md` "Test plan". | `artifacts/cycle-vehicle-wayfinding-and-prompts/playtest-evidence/{m151,m48,sampan,pbr,m2hb}-{open_frontier,a_shau_valley}-{hud,minimap,fullmap,compass}.png` (20), plus `negative-far-no-prompt.png` + `negative-in-vehicle-no-prompt.png` (2 negative cases), plus `summary.json` with per-target `matchedVehicleId` + `surfaces.{hud,minimap,fullmap,compass}.{wrote,note}` and `resolvedBackend` (captured by `scripts/capture-vehicle-wayfinding-shots.ts`; HUD prompt + minimap marker + full-map marker + compass chevron rendering each depend on the four R1 landings (PRs #278, #279, #280, #281) and the compass-wiring fix in commit 1 of this branch being in the build). | First in-world vehicle signposting cycle (closes `VEKHIKL-UX-1`). Owner sign-off blocks `VEKHIKL-UX-1` promotion to Closed in `docs/CARRY_OVERS.md`. Capture-script + doc skeleton landed under autonomous-loop posture; capture script tolerates absent `playerController.teleport`, absent `vehicleManager.spawnPlayerInNearestVehicle` dev surfaces, and absent prompt-text DOM probes — each capture reserves its screenshot path regardless. The `compass-vehicle-markers` R1 PR (#278) explicitly deferred the runtime wiring because its worktree was fenced to `src/ui/compass/**`; commit 1 of this branch lands the missing `StartupPlayerRuntimeComposer` wiring (`createCompassVehicleQuery` adapter over `VehicleManager.getAllVehicles()`) so the chevrons paint in the captured frames. |
| `cycle-skylut-resolution-bump` | (this cycle's close commit) | Walk the LUT-bump pre/post diff pair on (1) Open Frontier noon (the "random dark spots" report) and (2) A Shau midday flyover (the "skybox edge through terrain" report). Confirm the post-bump shots are free of bin-boundary banding and that `bump-summary-post.json` shows `horizonRow.monotonicUnder4 === true` and `fogVsSky.passesUnder5Pct === true` on both records. Confirm `combat120` p99 inside ±0.5 ms vs cycle #12 close (LUT bake is off the hot path). Real mobile-device walk: Pixel 5 + iPhone 12 emulation hold within 10% of cycle #12 baselines (29.02 / 28.88 avgFps). Full walk-list mirrored in `docs/playtests/cycle-skylut-resolution-bump.md` "Test plan". | `artifacts/cycle-skylut-resolution-bump/playtest-evidence/pre-openfrontier-noon.png`, `pre-ashau-noon.png`, `post-openfrontier-noon.png`, `post-ashau-noon.png` plus `bump-summary-pre.json` + `bump-summary-post.json` (captured by `scripts/capture-sun-and-atmosphere-shots.ts --lut-bump-check --prefix=<pre\|post>`; pre-bump captures depend on checking out `master@be953420` for the baseline half, post-bump runs against the cycle head — back-fill on master post-merge if needed). | First sky LUT resolution bump (closes `KB-SKY-LUT-BANDING`, the zero-cycle visual-quality follow-up to `KB-SKY-DEEP`). Owner sign-off records cycle-close acceptance for the LUT-bump fix to the "Open Frontier midday dark spots" + "skybox edge through terrain" user-reported visual artifacts. Capture-script extension + doc skeleton landed under autonomous-loop posture; capture script tolerates absent WebGPU adapter in headless Chromium (resolves to `webgpu-webgl-fallback`) and writes per-capture LUT-bump assertion blocks (`horizonRow`, `fogVsSky`) to `bump-summary-<prefix>.json`. Cycle #12 follow-up flag (b) — per-preset `computeSunDirectionAtTime` elevation envelope sanity check — recorded as an observation in the playtest memo; not fixed in this cycle. |
| `cycle-sun-and-atmosphere-overhaul` | (this cycle's close commit) | Walk the 5 scenarios × 4 TODs visual matrix (20 frames) + the 8 WebGPU/WebGL2 parity pair + the 5 night-red regression frames: (1) on each of ashau, openfrontier, tdm, zc, combat120 force `WorldBuilder.forceTimeOfDay` to the noon / golden / dusk / twilight slots and confirm the spike Section 4 visual targets (cobalt zenith at noon, mie-band stretching at golden, blood-orange dusk horizon, "keeper red" twilight vibe band with cool terrain lighting); (2) on a single scenario (openfrontier) repeat under `?renderer=webgl` and confirm visual parity to the human eye; (3) at midnight on each scenario confirm `renderer.moonLight.color` reads cool, NOT red-dominant; (4) A/B AGX vs ACES via the WorldBuilder console `toneMapping` field and record subjective preference; (5) walk a real mobile device against the steady-state perf-startup-mobile probe to confirm avgFps holds within 10% of cycle #2's baselines (Pixel 5 23.68, iPhone 12 28.30). Full walk-list mirrored in `docs/playtests/cycle-sun-and-atmosphere-overhaul.md` "Test plan". | `artifacts/cycle-sun-and-atmosphere-overhaul/playtest-evidence/visual-{ashau,openfrontier,tdm,zc,combat120}-{noon,golden,dusk,twilight}.png` (20), `parity-openfrontier-{noon,golden,dusk,twilight}-{webgpu,webgl}.png` (8), `nightred-{ashau,openfrontier,tdm,zc,combat120}-midnight.png` (5), plus `summary.json` with per-capture metadata + computed `parityDeltas[]` + `nightRedRegression[]` (captured by `scripts/capture-sun-and-atmosphere-shots.ts`; visual fidelity depends on sibling R1 PRs `night-red-fix` #270, `agx-tonemap-swap` #269, `tsl-preetham-fragment-port` #271 + R2 sibling PRs `sun-disc-and-aureole-tuning` and `per-scenario-exposure-recalibration` landing — back-fill on master post-merge if needed). | First sun + atmosphere overhaul (closes `KB-SKY-DEEP` and the HosekWilkieSkyBackend half of `konveyer-large-file-splits`). Owner sign-off records cycle-close acceptance for the visual-quality follow-up to the cycle #1 `KB-SKY-BLAND` close. Capture-script + doc skeleton landed under autonomous-loop posture; capture script tolerates absent `forceTimeOfDay` runtime override (falls back to direct `sunDirection` rotation for `combat120` which has no `todCycle`), absent WebGPU adapter in headless Chromium (resolves to `webgpu-webgl-fallback`), and absent `moonLight.color` surface (the night-red sampler returns null + records as a pass-skipped). Mobile real-device walk deferred to the owner sweep; emulation probes recorded in the PR body. |
| `cycle-ashau-edge-and-flow-tuning` | (this cycle's close commit) | Walk three A Shau visual fixes pre vs post: (1) north-edge flyover at altitude 1500 m heading north toward DEM boundary — confirm no vertical fins past the heightmap edge; the post-D3 taper ramps from boundary value to `DEM_EDGE_BASELINE_M = 0 m` over `DEM_EDGE_TAPER_RADIUS_M = 1500 m`; (2) valley-road wide shot on a hillside route corridor — confirm the route drapes over the slope rather than cutting a trench (slope-guard fires above 15 deg with 5 deg softness, flatten -> 0.0); (3) water acceptance dropped — hydrology + all water were stripped to first principles on 2026-06-09 (rework pending); skip the historical hydrology-mesh sampan smoke; (4) NPC nav smoke across the once-flattened corridor (stuck-rate must not regress > 50%); (5) optional real-device mobile walk against cycle #12 baselines (Pixel 5 29.02 / iPhone 12 28.88 avgFps; ±10% gate). Full walk-list mirrored in `docs/playtests/cycle-ashau-edge-and-flow-tuning.md` "Test plan". | Historical evidence: `artifacts/cycle-ashau-edge-and-flow-tuning/playtest-evidence/ashau-{north-edge-flyover,valley-road-wide,sampan-spawn-closeup}-{pre,post}.png` plus `summary-<pair-tag>.json`. (Water acceptance no longer applies after the 2026-06-09 water strip.) | First A Shau-specific edge + flow tuning cycle (closes `KB-DEM-EDGE-TAPER`; closes Stage D3 of cycle-2026-05-09-cdlod-edge-morph). Owner sign-off records cycle-close acceptance for the three R1 landings — `dem-edge-taper` (PR #275 + worker parity PR), `route-stamp-slope-guard` (PR #282), `ashau-water-enable` (PR #277 @ d0adbd9c). Capture-script + doc skeleton landed under autonomous-loop posture; script tolerates A Shau slow-stream (8 s settle) + absent WebGPU adapter in headless Chromium (resolves to `webgpu-webgl-fallback`). Three reviewer follow-up items captured in the playtest memo's "Cycle retro items" section (1: TerrainFlowCompiler `samplingRadius` not scaled by `flattenStrength`; 2: 4-tap slope sample directionally biased at DEM edges; 3: sampan coord `(-6895, 0, 4835)` brittle to hydrology bake regen). |
| `cycle-vekhikl-player-boarding-wire` | (this cycle's close commit) | Walk player boarding for all five drivable vehicle types: (1) on Open Frontier, locate the M151 jeep near the motor pool — walk within ~3 m, confirm HUD shows "Press F to board M151 Jeep", press F (confirm third-person follow camera engages + prompt hides), drive 10 s with W (confirm Ackermann steering), press F again (confirm player ejects to the +X side of the chassis, not under it, and prompt returns inside 6 m); (2) repeat for the M48 Patton at the motor-pool bay (depends on cycle #3 dedup landing) — drive 10 s with W + A/D skid-steer; pilot seat only this cycle (gunner swap deferred to `cycle-vekhikl-seat-swaps` hold-list); (3) walk to the M2HB tripod at the OF US FOB, press F to mount, confirm first-person barrel-axis camera + LMB fire intent reaches the M2HB system (cycle vekhikl-2 wired slew + fire — this cycle proves the mount), press F to dismount; (4) on A Shau Valley, locate the Sampan at the riverbank — confirm HUD prompt, drive 10 s with W (low power profile by design), exit beside the hull (riverbank or in-water step both valid); (5) repeat for the PBR at the US river outpost — pilot seat only this cycle (gunner swap to M2HB twin mounts deferred to same hold-list cycle); (6) mortar fallback (load-bearing) — walk somewhere with no vehicle in 6 m proximity, press F, confirm mortar fires (router's "boarding declined → forward to onMortarFire" path; if mortar fire breaks that's a router-gate regression); board any vehicle and confirm F-while-seated triggers exit (not mortar fire). Full walk-list mirrored in `docs/playtests/cycle-vekhikl-player-boarding-wire.md` "What the owner should walk". | `artifacts/cycle-vekhikl-player-boarding-wire/playtest-evidence/{m151,m48,sampan,pbr,m2hb}-{open_frontier,a_shau_valley}-{pre-press,post-press,post-exit}.png` (15 = 5 vehicles × 3 frames each), plus `summary.json` with per-target `matchedVehicleId` + `frames.{pre-press,post-press,post-exit}.{wrote,note}` + `boardCallResult` (true/false/absent/error) + `exitCallResult` + `resolvedBackend` (captured by `scripts/capture-vekhikl-player-boarding-shots.ts`; boarding round-trip depends on the five R1 landings (PRs #293/#297/#298/#288/#289/#296) + the sibling `vekhikl-board-system-updater-wire` SystemUpdater wire being in the build for the proximity-checker to latch the prompted vehicle id under the live game loop). | First player-boarding wire-up across all five drivable vehicle types (closes `VEKHIKL-UX-2`). Owner sign-off blocks `VEKHIKL-UX-2` promotion to Closed in `docs/CARRY_OVERS.md`. Capture-script + doc skeleton landed under autonomous-loop posture; capture script tolerates absent `playerController.boardingFactory` surface (probes both the split B field and the legacy `tryBoardNearestVehicle` callback), absent `playerController.teleport`/`setPosition` dev surfaces, and missing vehicle spawns (no live match → still writes the three frame slots) — each capture reserves its filename regardless. PNG run deferred to owner walk-through invocation per the R2 budget-discipline note in the playtest memo (same pattern as PR #295 motor-pool captures). L3 cross-category integration test at `src/integration/vehicle/board-five-types.test.ts` exercises the real factory + proximity checker + session controller against all five categories and passes 7/7 assertions. M48 + PBR gunner-seat swaps are explicitly out of scope (`cycle-vekhikl-seat-swaps` hold-list cycle). |
| `cycle-motor-pool-reflow-and-tank-dedup` | (this cycle's close commit) | Walk OF + A Shau motor-pool layout fixes pre vs post: (1) load Open Frontier, navigate to the airfield Main Motor Pool at `(155, 0, -1195)` — confirm four ground vehicles read as a working motor pool (staggered bays z ∈ [8, 18], yaw spread 72° from `π * 0.3` to `π * 0.7`, ≥ 1.5 m bounding-box clearance per pair); confirm AMMO/SUPPLY/FUEL crates flank the comms tower at x=-24 (no longer a second row behind vehicles); (2) confirm exactly ONE M48 silhouette at the OF motor pool (the real Tank IVehicle at `(183, 0, -1173)`) and zero M48s anywhere in the West FOB vicinity at `(-1025, 0, -760)` (pre-cycle-#3 the real Tank spawned there); (3) approach the motor-pool M48 within ~6 m and confirm "Press F to board M48 Patton" HUD prompt (depends on cycle #1 `cycle-vekhikl-player-boarding-wire` being on master; if not, note as cycle-sequencing observation, NOT a cycle-#3 defect); (4) load A Shau Valley, navigate to A Shau Main Motor Pool — confirm visually identical to pre-cycle-#3 (shared `motor_pool_heavy` was split into `motor_pool_heavy_of` + `motor_pool_heavy_ashau` because the OF reflow's M48 bay sat outside A Shau's 34 m footprint; A Shau half preserves the cycle-vekhikl-3-shipped layout); (5) minimap + M-key full-map sanity — confirm OF M48 glyph at motor pool world position (not at West FOB). Full walk-list mirrored in `docs/playtests/cycle-motor-pool-reflow-and-tank-dedup.md` "Test plan". | `artifacts/cycle-motor-pool-reflow-and-tank-dedup/playtest-evidence/of-motor-pool-{pre,post}.png` + `ashau-motor-pool-{pre,post}.png` (4 = 2 pairs) + `of-fob-no-tank.png` (single-state, post-only) + optional `of-motor-pool-tank-prompt.png` (`--include-prompt` flag, depends on cycle #1 boarding wire), plus `summary-<pair-tag>.json` with per-shot metadata (captured by `scripts/capture-motor-pool-shots.ts`; pre / post pairing driven by `--pair-tag=<pre|post>` flag run on `master@67969e60` baseline vs post-merge tip; A Shau anchor resolved at runtime via `worldFeatureSystem.getFeatures()` so the script tolerates minor reposition tweaks; capture PNGs deferred to owner walk-through invocation per the R2 budget-discipline note in the playtest memo). | First motor-pool layout + M48 dedup cycle (opens + closes `VEKHIKL-LAYOUT-1`). Owner sign-off records cycle-close acceptance for the two R1 landings — `of-tank-relocate-to-motor-pool` (PR #287, real M48 IVehicle to motor pool bay) + `motor-pool-heavy-reflow` (PR #290, prefab split with forced side-effects on `gameModeTypes.ts` + `scripts/check-terrain-visual.ts` for the new prefab IDs — user-approved scope expansion). Capture-script + doc skeleton landed under autonomous-loop posture; capture script tolerates A Shau anchor probe failure (logs to summary, skips the shot), headless Chromium WebGPU adapter absence (resolves to `webgpu-webgl-fallback`), and the cycle #1 boarding-wire dependency for the optional F-prompt close-up (capture still succeeds but the HUD panel is absent if cycle #1 not yet on master). One cycle-retro item recorded: prefab-ID additions ripple to `gameModeTypes.ts` + `scripts/check-terrain-visual.ts` in any future prefab-split cycle. |
| `cycle-2026-05-28-vehicles-aircraft-operable` (task `vehicle-occupancy-camera`, R1 keystone) | `04ce9c98`..close (this cycle's close commit) | Closes "I enter the new vehicles and the camera mounts under the car and I can't drive." (1) OF: walk to the jeep, board — confirm the camera snaps to 3rd-person behind/above the chassis (NOT under it); (2) drive W — confirm the camera follows the chassis and tracks yaw through turns; (3) exit F — confirm a clean return to first-person infantry view with no stuck 3rd-person frame; (4) repeat for the M48 (follow-cam tuned wider/higher). Full walk-list in `docs/playtests/vehicle-occupancy-camera.md` "Owner walk-through checklist". | Deferred — not captured this cycle. The existing `scripts/capture-m151-jeep-playtest-shots.ts` disables the engine loop + hand-poses the camera, so it bypasses the production `PlayerCamera.updateCamera` follow-cam under test; a valid live-loop smoke needs a new capture script (outside this task's 4-file scope). Intended dir: `artifacts/cycle-2026-05-28-vehicles-aircraft-operable/playtest-evidence/`. | Keystone fix: adds a `VehicleFollowCamera` provider slot to `PlayerCamera` (3rd-person branch between the flight branches and the first-person fallback); ground + tank adapters register/clear it in `onEnter`/`onExit`. Merge gate is the L2 `src/systems/player/PlayerCamera.test.ts` suite + green build, per autonomous-loop posture. Capture deferred per posture + owner "running lots of other stuff" (no headed GPU capture this cycle). |
| `cycle-2026-05-28-vehicles-aircraft-operable` (task `aircraft-armament`, R1) | `04ce9c98`..close (this cycle's close commit) | (1) **Huey door gun** — board a UH-1 with a crewed door-gun seat, get airborne, confirm the M60 engages nearby enemies with tracers; confirm it is silent parked and when the seat is unmanned. (2) **Fixed-wing nose cannon** — pilot an A-1/F-4, get airborne, hold LMB, confirm the nose cannon fires forward with tracers and ammo draws down; silent while parked. (3) **Friend-or-foe (the reported bug)** — fly over a mixed firefight, confirm aircraft hits damage ONLY enemies; a friendly directly under the gun takes ZERO damage. (4) Tracers render for both weapons. Full punch list in `docs/playtests/aircraft-armament.md`. | Deferred — IFF (the load-bearing bug) is covered by deterministic unit tests (`HelicopterWeaponSystem.test.ts`, `FixedWingModel.test.ts`, `FixedWingPlayerAdapter.test.ts`, `CombatantHitDetection.test.ts`), since "a friendly takes zero damage" is a damage-resolution assertion, not a visual one. No screenshot capture this cycle. Intended dir: `artifacts/cycle-2026-05-28-vehicles-aircraft-operable/playtest-evidence/`. | `combat-reviewer` APPROVE. Helicopter door-gun + IFF are fully wired in prod. **Known seam:** the fixed-wing nose cannon is fully implemented + unit-tested at the model+adapter layer, but the memo flagged the final live-LMB route (`PlayerCombatController.beginFire/endFire` → `isInFixedWing` branch) as belonging to the keystone vehicle/adapter wiring — at walk time, if LMB does not yet fire the nose cannon from a live cockpit, that is the documented seam, NOT a regression. |
| `cycle-2026-05-28-vehicles-aircraft-operable` (task `tank-crew-cannon-turret`, R2) | `04ce9c98`..close (this cycle's close commit) | Closes the functional half of "I'm not sure how to use the tanks." (1) Spawn/board the M48; (2) swap driver↔gunner seat and back (confirm the control target toggles); (3) as gunner, aim the turret + barrel (real geometry now, not the `m48_turret_placeholder`/`m48_barrel_placeholder` cylinders) and fire the cannon at a target; (4) confirm the shot resolves to damage via the existing weapon primitives. | Deferred — no dedicated capture script landed this cycle; the prior `scripts/capture-vekhikl-4-tank-shots.ts` covers analogous turret/cannon/aim frames and is back-fillable on master post-merge. Intended dir: `artifacts/cycle-2026-05-28-vehicles-aircraft-operable/playtest-evidence/`. | `combat-reviewer` APPROVE. `TankPlayerAdapter` previously hardcoded `canFirePrimary:false`/`weaponCount:0`; this task wires real `weaponCount`, seat-swap, cannon fire, and real turret/barrel geometry with correct pivot/muzzle transforms. Capture deferred per autonomous-loop posture + resource conservation. |
| `cycle-2026-05-28-vehicles-aircraft-operable` (task `tank-deploy-loadout-ux`, R2) | `04ce9c98`..close (this cycle's close commit) | Closes the discoverability half of "I'm not sure where the tanks are." (1) Open the deploy screen — confirm the tank appears as a selectable deploy/loadout choice and that picking it spawns the player into/at a tank; (2) confirm tank spawn-location markers render on the respawn map + minimap at the correct world positions; (3) near/in a tank, confirm the vehicle action bar shows a brief enter / seat-swap / fire controls hint. | Deferred — no screenshot capture this cycle. Intended dir: `artifacts/cycle-2026-05-28-vehicles-aircraft-operable/playtest-evidence/`. | UI/UX task (`DeployScreen`, `LoadoutTypes`, respawn map / minimap markers, `VehicleActionBar` hint). Pairs with `tank-crew-cannon-turret` (functional half) to fully close the owner-reported "I can't find / don't know how to use the tanks" gap. Capture deferred per autonomous-loop posture + resource conservation. |
| `cycle-terrain-compositor` | (this cycle's close commit) | Walk OF + A Shau terrain: (1) OF helicopter flyover of the main airfield at ~80 m — flat interior + smooth grade ramp + no random mountains. A Shau regression sentinel: airfield unchanged vs the 2026-05-19 cycle-ashau-edge-and-flow-tuning baseline. NOTE: the water/hydrology half of this cycle (compositor river capsules, watercraft-on-water checks) is moot as of 2026-06-09 — hydrology + all water were stripped to first principles; water returns when reworked in a future terrain/world-generator cycle that re-introduces a water level + real-time debug visualization. Full walk-list mirrored in `docs/playtests/cycle-terrain-compositor.md` "What to walk". | `artifacts/cycle-terrain-compositor/playtest-evidence/of-airfield-flyover-post.png`, `ashau-airfield-flyover-post.png`, plus `summary.json` with per-capture metadata (water/hydrology overlap shots dropped with the 2026-06-09 strip). | Cycle was **`attended`** posture (not `autonomous-loop`); this row is added for owner batching convenience, NOT as a deferred merge gate. Per the cycle brief's R3.2 scope: "this is owner-walk-through evidence, not a deferred merge gate". Per the cycle's non-goals any visual regression the owner finds at sweep time opens a follow-up `cycle-terrain-compositor-fix`; the merged commits are not reverted. Closes the OF "airfield random mountains + padding" owner-reported bug from 2026-05-27 (the "water on walls" half is superseded by the water strip). Design memo at `docs/rearch/TERRAIN_COMPOSITOR_SPIKE_2026-05-27.md`. |
| `phase3-bughunt` (optimal-arc autonomous run) | (this cycle's close commit) | Walk 5 feel-adjacent bug fixes: (1) **distant-battle audio** — stand 200-3000 m from an off-screen firefight, confirm gunfire ambience now plays (the `squad_engaged` cue was previously dead code); (2) **tank cannon reload** — fire the M48 90mm, immediately toggle driver↔gunner, confirm you CANNOT re-fire until the full reload elapses (seat-swap no longer re-arms it); (3) **NPC heli orbit** — observe an AI helicopter at a small/zero orbit radius, confirm smooth orbit (no NaN-frozen controls); (4) **door-gun/minigun fire rate** — fire the helicopter door gun at varying framerates, confirm rounds-per-second feels frame-rate-independent (was capped at 1 round/update); (5) **spectator auto-advance** — spectate, let the watched unit die, confirm the camera hands to the NEXT unit in viewing order (not back to index 0). | Deferred — all 5 covered by red→green behavior tests (`HelicopterWeaponFireRate`, `SpectatorAutoAdvanceOrder`, `PlayerMovementSwimHeadSample` + `AbstractCombatResolver`/`NPCPilotAI`/`TankPlayerAdapter` regressions); no screenshot capture. | 6 latent bugs confirmed + fixed under refute-by-default (~17 suspected refuted via empirical probes); combat-reviewer + terrain-nav-reviewer APPROVE; gate green (5239 tests). Feel-adjacent per `src/systems/vehicle/AGENTS.override.md` (green tests not sufficient sign-off for vehicle/flight feel). |
| `phase4a-godfile-burndown` (optimal-arc autonomous run) | (this cycle's close commit) | Low-risk locomotion spot-check after a behavior-preserving refactor: walk + sprint on flat ground, traverse a steep slope (confirm the steep-uphill contour slide still redirects rather than stalls), step onto/off ledges (support-normal slope-following), and jump — all should feel byte-identical to before. The `PlayerMovement` hot path was split into a facade + `movement/SteepTerrainFlow.ts` + `movement/SupportNormalSampler.ts` with the scalar math, branch order, and tuning constants moved verbatim. | Deferred — full suite (5239 tests incl. PlayerMovement + movement/ suites, 750/750 player) green; pure code-move, no logic/constant change. | God-file budget burndown: ModeStartupPreparer 825→121, PlayerMovement 736→672, DeployScreen 744→655, HUDElements 53→29 methods, all via facade-preserving splits (11 new sibling modules, all under budget). `lint:budget` 4 FAILs → 0. Only PlayerMovement is feel-sensitive enough to warrant a walk; the other three are init/UI. |
| `phase5-features` (optimal-arc autonomous run) | (this cycle's close commit) | Low feel-risk deploy-screen surface: on the deploy/respawn screen for Open Frontier and A Shau, confirm (1) the "CREW A VEHICLE" panel now renders (it was built+tested but `display:none`/unwired before) and reads cleanly on PC AND mobile; (2) the deploy-map shows the M48 marker at the real motor-pool (OF) / valley-road (A Shau) anchor; (3) selecting a vehicle is purely INFORMATIONAL (logs/focuses, does NOT teleport or crew the player — deploy-into-vehicle is deferred); (4) Team Deathmatch + Zone Control show no panel/marker. | Deferred — behavior-tested (`PlayerRespawnManager.test.ts`, `DeployScreenVehicleCatalogue.test.ts`); informational-only wiring, no combat/flight/movement feel changed. | Phase 5 scope confirmed aviation AVIATSIYA-4/5/6/7 + UX-1/2/3/4 largely ALREADY BUILT (DIRECTIVES rows realigned); only this one bounded UX gap (wire the already-built CREW-A-VEHICLE panel + markers into the live deploy flow) was implemented. 6 design-heavy gaps deferred with plans in `docs/rearch/PHASE5_FEATURE_SCOPE_2026-05-31.md`. |
| `task/combat-convergence-stall-fix` | `ff83f37e` | NPC convergence movement feel: in a 120-NPC Open Frontier / A Shau crush, watch NPCs converge on a contested point — they should not visibly freeze/bounce in place; contour-slide along blocking micro-terrain should look the same as before (the re-score cache is heading-identical per side); a squad packed at a choke that gives up (terminal "hold") should walk *away* from the crowd before re-engaging rather than re-piling instantly. Only `NpcLodConfig.stallDispersalEnabled` (default on) changes default behavior; `crowdStallStaggerEnabled` is default OFF. | Deferred — 4 behavior tests (contour-sampling amortization, stale-route serve under budget exhaustion, dispersal direction + delayed re-eval, stagger on/off cadence); full suite green (5260). No screenshot capture. | Only #1 (hold dispersal) is feel-sensitive and it fires on the rare terminal-hold path; #2/#3 are behavior-preserving perf changes; #4 is opt-in (off). Frame-time certification deferred to the STABILIZAT-1 quiet-machine refresh (local captures contended). |
| `cycle-combat-p99-attribution` (task `crowd-stall-stagger-enable`) | (this cycle's close commit) | Movement-feel sign-off on flipping `NpcLodConfig.crowdStallStaggerEnabled` default ON. In a 120-NPC Open Frontier / A Shau point-blank crush, watch contour-stalled NPCs packed in a friendly crowd: they coast (integrate velocity + re-ground) on every other tick instead of running the full terrain solve. Confirm there is NO visible micro-stutter / jitter on the staggered NPCs and that an isolated or freely-moving NPC is visually unaffected (the coast only arms for stalled + crowded high-LOD NPCs). Compare against `task/combat-convergence-stall-fix` close feel — route quality must not regress (no new freezes, no contour↔backtrack oscillation). | Deferred — covered by L1 behavior tests in `src/systems/combat/CombatantMovement.test.ts` (default pinned ON, 50% coast cadence, isolated-NPC never coasts, coast integrates velocity not a freeze) + the existing route-quality/determinism coverage. Frame-time certification (combat120 p99 ≤35ms) is itself deferred to the STABILIZAT-1 quiet-machine refresh — local captures are contended. No screenshot capture this cycle. | Only the stagger flag is an intended default behavior change; the contour height-sample dedupe in the same cycle is byte-identical (perf-only, determinism-proven). The flag arms only on the stalled+crowded high-LOD path, off today specifically to protect feel, so the walk is a smoothness check on that narrow path. |
| `combat-movement-stall-tail` (task `contour-sticky-hysteresis`) | (this change's commit; default-OFF) | Movement-feel sign-off BEFORE flipping `NpcLodConfig.contourStickyHysteresisEnabled` default ON. In a 120-NPC Open Frontier / A Shau crush at a contested zone behind blocking micro-terrain, toggle the flag ON (Tweakpane `` ` ``) and watch contour-stalled NPCs: they should commit to ONE go-around side and walk clear of the lip instead of flip-flopping in place. Confirm (1) no NPC visibly "hugs the wrong wall" for more than ~1 s before the 1200 ms reroute re-paths it (reviewer Note A — +1.0 stickiness trades flip-rate for slower side-abandonment; if it reads badly, 0.5-0.75 is the intermediate value to try); (2) route quality does not regress vs the `crowd-stall-stagger-enable` close (no new freezes, no contour↔backtrack oscillation, no permanent stalls); (3) NPCs traversing CURVED ridgelines (not just the straight asymmetric lip the unit harness uses) still abandon a side when the terrain genuinely improves on the other. | N/A (L2 A/B behavior test in `src/systems/combat/CombatantMovement.test.ts` — flag-OFF byte-identical, flag-ON cuts contour side-flips ≥50% + clears the lip on an asymmetric ridge). Frame-time certification (combat120 p99 ≤35 ms) deferred to the STABILIZAT-1 quiet-machine refresh. | Ships DEFAULT-OFF (zero behavior change until this walk passes), mirroring the `crowd-stall-stagger-enable` rollout. combat-reviewer APPROVE-WITH-NOTES (default-off byte-identity confirmed; 1200 ms reroute backstop survives; 200 ms re-score cache interaction intended). Reviewer Note B: before flip-on, add an L3 sloped-terrain integration scenario so the p99 win is observed end-to-end, not just in the synthetic ridge. Lever is the residual `combat-movement-stall-tail` split from DEFEKT-3 (cover search proven cheap; per-tick terrain dedup proven a ~12% mirage by L1 measurement). |
| `DIZAYN-4` Field Journal frontend (campaign 2026-06-03) | `c3ae1782` | Walk the FULL Field Journal frontend on PC AND a real phone: (1) title / mode-select / deploy read as Field Journal (manila paper, typewriter ink, stamp-red + field-green); (2) in-game HUD — ammo / vitals / objective / timer / tickets / kill-feed / minimap / compass / crosshair all FJ, NO overlap or off-canvas at supported PC + phone sizes, no page-scroll; (3) touch controls (joystick / fire / ADS / reload / grenade / interaction / menu) thumb-reachable + no overlap at 390px / notch / landscape; (4) full tactical map = manila sheet with sepia shaded-relief + FJ zones/markers; minimap = dark ink chip with green/red factions; (5) vehicle HUDs (helicopter / fixed-wing) FJ; (6) ALLIED=green, HOSTILE=red everywhere; (7) no boot "small box" flash before the title. | In-session browser-preview confirmation done for title / mode / deploy (PC+phone) / in-game HUD / settings / maps; the in-game HUD + touch + vehicle HUDs render only in live gameplay → owner walk is the final visual + overlap/placement pass (campaign Principle 5). | FJ campaign shipped 8 cycles + follow-ups; old styling purged, fallbacks stripped, all CI-green on master. `check:hud` is environmentally broken locally (headless WebGPU-fallback boot) but validates in CI. |
| `cycle-air-support-radio` (SVYAZ-3) | `fa273ebd` | Call in air support: press `T` to open the radio (look at the target area first — the call-in marks where you were looking), pick an asset (A-1 Napalm / AC-47 Orbit / etc.), confirm the NPC sortie flies in and strikes the marked point. Confirm: (1) per-asset cooldown shows + blocks re-call; (2) a napalm or AC-47 (spooky) strike called onto a mixed firefight does ZERO damage to friendlies (IFF); (3) the sortie runs in along your line of sight. NOTE: `rocket_run` (A-1 Rockets / Cobra) IFF is a known follow-up — those rockets can still hit friendlies until the spawned `rocket_run` IFF task lands. | N/A (deterministic IFF + dispatch tests stand in: CommandInputManager dispatch-with-faction; Napalm/Spooky faction threading). | Radio call-in wired end-to-end + IFF for the two direct-damage sortie types. Marking UX is "look-to-mark" (camera-forward ground pick); the spike flagged tactical-map-click as an alternative the owner may prefer — flag at playtest. |
| `cycle-deploy-loadout-flow` (UX-2/3/4) | `86ee640d` | Walk the deploy screen on PC AND a real phone: (1) UX-2 — tap spawns on the map (especially adjacent A Shau zones zoomed out) and confirm coarse taps reliably select the NEAREST spawn; (2) UX-3 — the loadout sheet shows the faction's available weapons/equipment as chips (reduced for ARVN/NVA/VC), the AMMO LOAD slot cycles Standard/Extended/Heavy, 44px touch targets; confirm spawning with EXTENDED/HEAVY gives more reserve ammo in-game; (3) UX-4 — the spawn list shows CLEAR/WARM/HOT/EXPOSED threat bands (OF/A Shau war modes; ZC/TDM read CLEAR by design); (4) nothing off-canvas / no page-scroll, pinned DEPLOY stays visible. | N/A (DOM + L1/L2 unit tests: 11 hit-test, 7 threat, 29 ammo, faction-availability). `check:mobile-ui` validates single-scroll-owner + no-overflow in CI. | Function layer of UX-2/3/4 (the FJ visual layer shipped in the DIZAYN-4 campaign). `deploy-first-frame-continuity` is a verify-only sub-item: confirm the spawn-loading overlay bridges confirm→first frame + decide whether a "spawned hot" first-frame HUD cue is wanted. Selectable ammo is fence-safe (IAmmoManager untouched). |
| `SVYAZ-4` squad-command rebuild (Stages 1-4) | `5251e391` | Command your squad and confirm orders SURVIVE combat: (1) look at a point and issue HOLD (Shift+2) / ATTACK (Shift+6) / PATROL — the marker pings where you LOOK (not your feet) and the squad goes there; (2) HOLD — squad engages enemies near the anchor but does NOT chase a baited enemy far past it, and drifts back to the anchor; (3) ATTACK — squad advances onto the marked point, engaging en route; (4) FALL BACK — squad breaks contact and runs to rally (you, or a marked point) and only fires back when pinned (recently hit); (5) command as OPFOR (NVA/VC player squad) and confirm orders are followed (was a silent no-op before); (6) mark an order on un-walkable ground and confirm the squad still reaches it (anchor snaps to navmesh). | N/A (L1/L2/L3 behavior tests: SquadOrderPosture, persistence-leash L3, posture L3, pinned-fire multi-tick L3; full suite 5390 green). | Full squad-command rebuild over 4 stages, all on master, combat-reviewer APPROVE per stage. Ships default-on (dead code unless the player commands a squad). Design: `docs/rearch/SQUAD_COMMAND_SVYAZ4_SPIKE_2026-06-03.md`. Deferred follow-ups: a shared `isPinned` helper (the recency check is duplicated in CombatantAI + SquadOrderPosture); cover/suppression-flank leash post-filter + ping polish. |
| `AVIATSIYA-2` AC-47 takeoff bounce | `5251e391` | Fly the AC-47 "Spooky": take off at LOW pitch from the airfield and confirm the airframe rolls smoothly into a climb with NO vertical pop / single-bounce (it should stay glued to the runway, accelerate past stall speed, then climb away). Confirm a normal higher-pitch rotation still feels snappy, and compare to the A-1/F-4. | N/A (behavior test `Airframe.groundRolling.test.ts` asserts no bounce signature + climb-away; fails on the old impulse). | Root cause: a fixed `max(4.5, newFwd*0.12)` ground-clearing impulse popped the marginal-lift gunship (commits below its own stall speed). Fix scales the impulse by earned lift margin, hard-capped at 3.0 m/s (peak liftoff vy 4.51→0.19). Feel-adjacent — owner sign-off on liftoff feel + the cap tuning. |
| `cycle-2026-06-04-deploy-zone-vehicle` (UX-5 `loadout-deploy-equip-match`) | `e0144444` | Deploy with a NON-default loadout and confirm the in-hand weapon matches your selection: (1) initial deploy — pick a non-default primary on the deploy screen, spawn, confirm you hold exactly that weapon; (2) die and respawn (same faction) without re-touching the loadout — confirm you still spawn with your selected primary, not a default or your previous in-hand slot; (3) switch faction (US↔VC) on the deploy screen and confirm the equipped weapon tracks the new faction's selected loadout; (4) the original repro — die WHILE a weapon switch is animating, respawn, confirm the spawned weapon is still the selected primary (this was the drop-in-flight-switch bug). | N/A (L3 `src/systems/player/LoadoutDeployEquip.test.ts` asserts equipped == selected for initial + 2 respawns + faction switch + mid-switch death; full suite green). | Root cause was `WeaponRigManager.startWeaponSwitch` rejecting a new switch while one was in flight, so a stale switch won when the player died mid-swap. Fix = last-requested-wins. No reviewer gate (player path, not combat/terrain). |
| `cycle-2026-06-04-deploy-zone-vehicle` (DEFEKT-7 `zone-base-ditch-placement`) | `fb371129` | Boot Zone Control and confirm placement: (1) both home bases (US + OPFOR) sit on flat ground on their firebase/bunker pad, NOT in a depression; (2) all three capture zones (Alpha/Bravo/Charlie) rest on flat ground; (3) spawn at your home base and confirm you (and enemies at the OPFOR base) appear on flat ground, not down in a hole; (4) sanity-walk the three pre-baked ZC seed variants if convenient. | N/A (L3 `src/systems/world/ZoneDitchPlacement.test.ts` over the real ZC config + a ditch height-field stub: bases stay on-pad, capture zones climb out, readiness guard suppresses nudge on unready terrain; full suite green). | terrain-nav-reviewer APPROVE-WITH-NOTES. Fix = readiness guard mirroring `canUseTerrainAt` + `validateTerrain:false` on the stamp-flattened home bases + wider nudge search. Reviewer follow-up for `docs/MOVEMENT_NAV_CHECKIN.md`: the unconditional spiral pre-pass in `ZoneInitializer.findSuitableZonePosition` still lacks a readiness guard (harmless today — spawns read the authored config coord — but flag if the ZoneManager base-zone copy ever becomes authoritative). |
| `cycle-2026-06-04-deploy-zone-vehicle` (VEKHIKL-5 `vehicle-board-drive-e2e`) | `f63b0da5` | Walk the M151 jeep (and M48 tank) board→drive: (1) approach the jeep, press F to board — confirm the camera/player snaps into the DRIVER seat (forward), NOT behind/at the rear of the chassis; (2) confirm the jeep is resting ON the terrain at spawn, not clipped half-under it; (3) press W to drive — confirm it actually accelerates forward; A/D steers (Ackermann); (4) press F to dismount — confirm you eject beside the chassis (not under it); (5) repeat the board → driver-seat anchor → dismount check on the M48 (its driver seat is forward-of-center too). | N/A (L3 `src/systems/vehicle/VehicleBoardDriveE2E.test.ts`: board seats at the driver offset, vehicle rests at terrain+axleOffset, throttle moves it forward, steer changes heading, dismount frees the seat; jeep + M48; full suite green). | No reviewer gate (vehicle path). Fixes: seat-world-offset board (use the locked `seatIndex` + rotated `localOffset`) and `GroundVehiclePhysics.conformToTerrain()` on `setTerrain` so the jeep is grounded — and therefore drivable, since drive force is gated on `isGrounded` — from frame 0. Note: production drive path is `vehicle.update(dt)`, not the adapter's `stepPhysics`; M48 uses `TrackedVehiclePhysics` (no rest-height conform added there this cycle). |

## Walk-through protocol (for the owner)

1. Pull master.
2. For each row above:
   - Open the Playwright smoke screenshots under
     `artifacts/cycle-<slug>/playtest-evidence/`.
   - Spin up dev preview (`npm run dev`) and walk the feature's
     golden path manually.
   - If the feature reads "right":
     - Move the row to the "Walked & accepted" section below with a
       date and a one-line note.
   - If the feature reads "wrong":
     - Move the row to "Walked & rejected" below with a one-line
       cause.
     - Open a follow-up cycle brief at
       `docs/tasks/cycle-<slug>-fix.md` and queue it in the active
       campaign manifest.

## Walked & accepted

(Empty.)

## Walked & rejected

(Empty.)

## Reference

- [.claude/agents/orchestrator.md](../.claude/agents/orchestrator.md)
  §"Autonomous-loop posture" — defines what triggers an append here.
- [docs/AGENT_ORCHESTRATION.md](AGENT_ORCHESTRATION.md)
  §"Autonomous-loop posture" — cross-tool view of the same rules.
- [docs/CAMPAIGN_2026-05-13-POST-WEBGPU.md](archive/CAMPAIGN_2026-05-13-POST-WEBGPU.md)
  — current campaign manifest declaring `posture: autonomous-loop`.
